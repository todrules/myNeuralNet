

import { Injectable } from '@angular/core';
import { AnnotateTextResponse } from '../utils/models';

@Injectable()
export class GenerateResponse {
  
  constructor() {
  
  }
  
  getReply(parsedText: AnnotateTextResponse) {
  
  }
}
